#ifndef TYPES_HPP
#define TYPES_HPP

#include <SDL2/SDL.h>
#include "Vector2D.hpp"

using PosType = float;
// using PRL_Rect = struct PRL_Rect{ float x, y, w, h; };
using PRL_Rect = SDL_FRect;

#endif // TYPES_HPP