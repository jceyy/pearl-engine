#ifndef ANIMATION_HPP
#define ANIMATION_HPP

#include <iostream>
#include <vector>
#include <SDL2/SDL.h>


/*
class Animation {
public:
    Animation();
    Animation(const std::string& animationPath);
    // Animation(int i, int frames, int fps);

private:
    // size_t index;
    std::vector<SDL_Rect> animFrames_;
    std::vector<SDL_Texture*> animTextures_;
    size_t frames;
    float fps;
};*/

#endif // ANIMATION_HPP